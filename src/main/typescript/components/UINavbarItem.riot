<button class="material-icons mdc-top-app-bar__action-item mdc-icon-button" aria-label="{props.label}">

    {props.icon ? props.icon : props.label}

    <script>
        import { UINavbarItem } from "./UINavbarItem.ts";
        export default new UINavbarItem();
    </script>
</button>
